package com.classmanager.cms_backend.enums;

/**
 * Timing slot for a batch.
 * MORNING and EVENING are the two standard slots.
 * CUSTOM allows the admin to define any other timing.
 */
public enum BatchTiming {
    MORNING,
    EVENING,
    CUSTOM
}
